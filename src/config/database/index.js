/**
 * A module that exports a configuration class for connecting to a MongoDB database.
 * @module MongoDatabase
 */

const MongoDatabaseConfig = require("./mongodb.config");

module.exports = { MongoDatabaseConfig };
