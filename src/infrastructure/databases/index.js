/**
 * A module that exports a configuration class for handling MongoDB database connection.
 * @module MongoDatabase
 */

const MongoConnection = require("./mongodb.connection");

module.exports = { MongoConnection };
